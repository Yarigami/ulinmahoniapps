import 'package:flutter/material.dart';

class UmiPage extends StatelessWidget {
  const UmiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('UMI Page')),
    );
  }
}
